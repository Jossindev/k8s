apiVersion: apps/v1
kind: Deployment
metadata:
  name: resources-service
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.replicaCount.resourcesService }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: resources-service
  template:
    metadata:
      labels:
        app: resources-service
    spec:
      containers:
        - name: resources-service
          image: {{ .Values.resourcesService.image }}
          imagePullPolicy: {{ .Values.resourcesService.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.resourcesService.containerPort }}
          envFrom:
            - configMapRef:
                name: resources-service-config
          env:
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: POSTGRES_USER
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: POSTGRES_PASSWORD
          startupProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.resourcesService.containerPort }}
            failureThreshold: 60
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: {{ .Values.resourcesService.containerPort }}
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: {{ .Values.resourcesService.containerPort }}
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: resources-service
  namespace: {{ .Values.namespace }}
spec:
  type: NodePort
  selector:
    app: resources-service
  ports:
    - port: {{ .Values.resourcesService.containerPort }}
      targetPort: {{ .Values.resourcesService.containerPort }}
      nodePort: {{ .Values.resourcesService.nodePort }}
